= simple_form_for form, :html => {:class=>'form-horizontal'} do |f|
  %legend= "#{(f.object.new_record? ? 'Create' : 'Edit')} #{f.object.class.model_name.human}"
  .form-inputs
    = f.input :name
    = f.input :url
  .form-actions
    = f.button :submit, :class=>'btn-primary'
    
