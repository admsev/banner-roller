= simple_form_for [parent, @banner], :html => {:class => 'form-horizontal'} do |f|
  = f.input :name
  = f.input :url
  = f.input :image
  = f.input :show_stock
  = f.input :click_stock
  .controls
    = f.button :submit, :class=>'btn-primary'
